<h1>Add a review for <%= @restaurant.name %></h1>

<%= simple_form_for([@restaurant, @review]) do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: 0..5 %>
  <%= f.submit 'add a review', class: 'btn btn-primary w-100' %>
<% end %>

<%= link_to 'back to list', restaurants_path%>

